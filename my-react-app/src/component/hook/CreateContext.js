import React from "react";

const CreateContext = React.createContext({
  // auth: null,
  // login: () => {console.log("in Create Context")},
  // logout: () => {console.log("in Create Context");},
});

export default CreateContext;
